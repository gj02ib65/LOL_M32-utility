services:
  m32-sync:
    build:
      context: . # The build context is the repo root (needed to see /src and /containers)
      dockerfile: containers/app/Dockerfile
    container_name: m32-sync-utility
    restart: always
    # host networking is required for zero-latency UDP communication with the M32 consoles.
    # When network_mode is "host" the container shares the host network stack directly.
    # No ports: mapping is needed or used in this mode.
    network_mode: "host"
    volumes:
      - m32_church_data:/data
      # The lol_m32-flows repo is mounted read-only as the flows source.
      # For local dev this assumes sibling directories:
      #   C:\projects\
      #     m32-utility\    ← this repo
      #     m32-flows\      ← lol_m32-flows repo checkout
      - ../m32-flows:/opt/m32-flows-src:ro
    environment:
      - TZ=America/Chicago
      - NODE_RED_ENABLE_PROJECTS=false
      - CONFIG_FILE_PATH=/data/mixer_config.json
    command: --settings /opt/m32-logic/settings.js

volumes:
  m32_church_data:
